import { authenticatedRequest } from "./api-client"; // Re-export authenticatedRequest for use in route loaders/actions

// Re-export authenticatedRequest for use in route loaders/actions
export { authenticatedRequest };
